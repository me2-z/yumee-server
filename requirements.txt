# Yumee Signaling Server Dependencies
# =====================================
# These are pinned to specific versions for stability on Render.com
# Compatible with Python 3.11 (specified in render.yaml)

# Core Flask framework
Flask==3.0.0

# Flask-SocketIO for WebSocket support (using threading mode)
Flask-SocketIO==5.3.6

# CORS support for cross-origin requests
flask-cors==4.0.0

# WebSocket dependencies
python-socketio==5.9.0
python-engineio==4.8.0

# WSGI server (Render uses this)
gunicorn==21.2.0

# Additional utilities
Werkzeug==3.0.1
click==8.1.7
itsdangerous==2.1.2
Jinja2==3.1.2
MarkupSafe==2.1.3
bidict==0.22.1

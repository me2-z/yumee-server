# Yumee Signaling Server Dependencies
# =====================================
# These are pinned to specific versions for stability on Render.com

# Core Flask framework
Flask==3.0.0

# Flask-SocketIO for WebSocket support
Flask-SocketIO==5.3.6

# CORS support for cross-origin requests
flask-cors==4.0.0

# Python Socket.IO client (for testing if needed)
python-socketio==5.9.0

# WebSocket dependencies
python-engineio==4.8.0
websocket-client==1.6.4

# WSGI server (Render uses this)
gunicorn==21.2.0

# Eventlet or gevent for async support (eventlet is more stable on Render)
eventlet==0.33.3

# Additional utilities
Werkzeug==3.0.1
click==8.1.7
itsdangerous==2.1.2
Jinja2==3.1.2
MarkupSafe==2.1.3
bidict==0.22.1
six==1.16.0
dnspython==2.4.2
greenlet==3.0.1
